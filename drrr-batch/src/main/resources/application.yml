spring:
  profiles:
    group:
      test:
        - test
      local:
        - local
      local1:
        - local1
      dev:
        - develop
      main:
        - main
      batch:
        - batch
    default: batch
  batch:
    job:
      name: ${job.name:NONE}

