spring:
  profiles:
    group:
      test:
        - test
      local:
        - local
      dev:
        - develop
      main:
        - main
      batch:
        - batch
    default: batch
  batch:
    job:
      name: ${job.name:NONE}

