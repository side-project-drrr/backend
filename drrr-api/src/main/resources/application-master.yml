server:
  port: 8081
spring:
  application:
    name: dr--r--r--
  main:
    allow-bean-definition-overriding: true
  data:
    redis:
      time-to-live: 3600 # 데이터 유지 시간(sec) 1시간
      host: 192.19.199.102
      port: 6379
  kafka:
    consumer:
      bootstrap-servers: 192.19.199.101:9093
      auto-offset-reset: earliest
      enable-auto-commit: false
      auto-commit-interval: 10
    producer:
      bootstrap-servers: 192.19.199.101:9093
    backoff:
      interval: 1000
      max_failure: 3
  datasource:
    url: jdbc:mysql://drrr-database.cxucsa028e73.ap-northeast-2.rds.amazonaws.com:3306/drrr_app?profileSQL=true&allowPublicKeyRetrieval=true&useSSL=false&characterEncoding=UTF-8&serverTimezone=Asia/Seoul&maxQuerySizeToLog=999999
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: drrr_app
    password: drrr!@#application
    hikari:
      connection-timeout: 300000
  jpa:
    hibernate:
      #애플리케이션 로딩 시점에 다 드랍하고 다시 생성
      ddl-auto: none
    properties:
      hibernate:
        jdbc:
          time_zone: Asia/Seoul
        format_sql: true
        use_sql_comments: true
  batch:
    jdbc:
      initialize-schema: ALWAYS
  security:
    oauth2:
      client:
        registration:
          github:
            client-id: classpath:security-storage-api/social/github/github.key
            client-secret: classpath:security-storage-api/social/github/github.pub
            redirect-uri: http://localhost:8081/
          kakao:
            client-id: classpath:security-storage-api/social/kakao/client_id.key
            redirect-uri: http://localhost:8081/
            client-authentication-method: POST
            authorization-grant-type: authorization_code
        provider:
          kakao:
            authorization-uri: https://kauth.kakao.com/oauth/authorize
            token-uri: https://kauth.kakao.com/oauth/token
            user-info-uri: https://kapi.kakao.com/v2/user/me
            user-name-attribute: id
kakao:
  private.key: classpath:security-storage-api/social/kakao/client_id.key

jwt:
  private.key: classpath:security-storage-api/jwt/jwt.key
  public.key: classpath:security-storage-api/jwt/jwt.pub
management:
  endpoints:
    web:
      exposure:
        include: "prometheus"
  metrics:
    distribution:
      percentiles-histogram:
        all: true
  otlp:
    endpoint: http://192.19.199.105:4317
    metrics:
      export:
        url: http://192.19.199.105:4318/v1/metrics
    tracing:
      endpoint: http://192.19.199.105:4318/v1/traces
  tracing:
    sampling:
      probability: 1.0
opentracing:
  jaeger:
    service-name: dr--r--r-- # jaeger service name
    udp-sender:
      host: 192.19.199.106 # jaeger server ip
      port: 6831           # jaeger udp port
    enabled: true
    log-spans: true